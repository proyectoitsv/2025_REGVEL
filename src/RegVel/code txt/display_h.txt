//=== display.h ===
/*
 * Módulo Visual Simplificado
 * Maneja: Display 7-Segmentos (Driver 4511) y LEDs de Estado
 */
#ifndef DISPLAY_H
#define DISPLAY_H

#include <Arduino.h>

// Inicializa pines
void disp_init();

// Refresco multiplexado (Llamar en loop PRINCIPAL sin falta)
void disp_mux();

// Actualiza el indicador visual del límite (LEDs)
void disp_set_limit_indicator(uint8_t limit_idx);

#endif